# GraphQL + TypeScript Auth Starter

This is a minimal starter that demonstrates GraphQL with TypeScript and an authentication flow (signup, login, me) returning a JWT.

Setup:

- Copy `.env.example` to `.env` and provide a `JWT_SECRET` value.
- Install dependencies: `npm install`
- Start in dev mode: `npm run dev`
